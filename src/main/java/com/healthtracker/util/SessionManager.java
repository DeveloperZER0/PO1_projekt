package com.healthtracker.util;

import com.healthtracker.model.User;

public class SessionManager {
    // singleton, przechowuje zalogowanego User currentUser
    // metody getCurrentUser(), setCurrentUser(User)
}
